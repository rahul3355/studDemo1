<h1>IndexPage</h1>

@*@model IEnumerable<studDemo1.Models.StudentContext>

<br />
<br />

@using(Html.BeginForm("Index", "Home", FormMethod.Get)){
    @Html.TextBox("searching")<input type="submit" value="Search"/>
}

<table>
    <thead>
        <tr>
            <th>Student ID</th>
            <th>Student Name</th>
            <th>Student Class</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="3" style="color:red">
                    No match
                </td>
            </tr>
        }
        else
        {
            foreach(var item in Model)
            {
                <tr>
                    <td>@item.StudentTable</td>
                    
                </tr>
            }
        }
    </tbody>
</table>*@